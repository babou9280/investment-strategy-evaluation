# H1 — strict numeric validation

Status: implemented and independently validated on `codex/h1-strict-numeric-validation`; pending pull-request review and merge into `breaktest-bootstrap`.

## Scope delivered

- deterministic materialization from the immutable v0.2 bundle ;
- fail-closed classification of mandatory numeric sources ;
- unambiguous PnL/return derivation with provenance flags ;
- whole-batch rejection for invalid rows ;
- Node regression tests and focused Chromium integration test ;
- canonical methodology, quality, state, decision and audit updates.

## Evidence

See `docs/validation/H1_STRICT_NUMERIC_VALIDATION.md`.

No other audit defect was corrected in this task.
